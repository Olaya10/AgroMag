server:
  port: 9000

spring:
  application:
    name: gateway-service

# El gateway utiliza rutas estáticas definidas en GatewayServiceApplication.
# No hay cliente de descubrimiento configurado en este microservicio.
